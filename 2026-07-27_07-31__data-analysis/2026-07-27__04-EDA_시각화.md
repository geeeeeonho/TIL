# Matplotlib·Seaborn 시각화 정리

## 1. 기본 구조

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
```

* **Figure**: 그래프 전체를 담는 도화지
* **Axes**: Figure 안에서 실제 그래프가 그려지는 칸

![Figure와 Axes 구조](sandbox:/mnt/data/figure_axes_structure.png)

```python
# Figure 1개, Axes 1개
fig, ax = plt.subplots(figsize=(8, 4))

ax.plot(x, y)
ax.set_title('제목')
ax.set_xlabel('x축')
ax.set_ylabel('y축')

fig.tight_layout()
plt.show()
```

여러 그래프를 만들면 `axes[행, 열]`로 위치를 지정함.

```python
fig, axes = plt.subplots(2, 2, figsize=(12, 8))

axes[0, 0].plot(x, y)
axes[0, 1].bar(x, y)
axes[1, 0].scatter(x, y)
axes[1, 1].hist(y)

fig.tight_layout()
plt.show()
```

> 함수 이름은 `plt.subplots()`임. `plt.Subplot()`과 다름.

---

## 2. Matplotlib과 Seaborn

| 구분    | Matplotlib | Seaborn         |
| ----- | ---------- | --------------- |
| 역할    | 그래프의 기본 바탕 | 통계 그래프를 쉽게 작성   |
| 입력    | 배열·집계 결과   | DataFrame과 열 이름 |
| 그룹 구분 | 직접 처리      | `hue=`로 자동 구분   |
| 세부 설정 | 자유도가 높음    | 간단하고 보기 좋음      |

보통 다음처럼 함께 사용함.

```python
fig, ax = plt.subplots(figsize=(7, 4))

sns.scatterplot(
    data=gym,
    x='운동시간',
    y='소모칼로리',
    hue='프로그램',
    ax=ax
)

ax.set_title('운동시간과 소모칼로리')
plt.show()
```

* 그래프 그리기: 주로 Seaborn
* 제목·축·범례·전체 배치: Matplotlib

---

## 3. 그래프 선택

![차트 선택 가이드](sandbox:/mnt/data/chart_selection_guide.png)

| 확인 목적     | 데이터         | 함수                      |
| --------- | ----------- | ----------------------- |
| 값의 분포     | 수치형 1개      | `histplot`, `kdeplot`   |
| 중앙값·이상치   | 수치형 1개      | `boxplot`, `violinplot` |
| 범주별 개수    | 범주형 1개      | `countplot`             |
| 그룹별 평균    | 범주형×수치형     | `barplot`               |
| 그룹별 분포    | 범주형×수치형     | `boxplot`, `violinplot` |
| 두 수치의 관계  | 수치형×수치형     | `scatterplot`           |
| 시간에 따른 변화 | 시간×수치형      | `lineplot`              |
| 집계 결과 비교  | 범주형×범주형→수치형 | `heatmap`               |
| 여러 수치 관계  | 수치형 여러 개    | `pairplot`              |

---

# 4. 단변량 분석

변수 하나의 분포나 개수를 확인함.

## 수치형 분포

### 히스토그램

값을 구간으로 나누어 개수를 표시함.

```python
sns.histplot(
    data=gym,
    x='소모칼로리',
    bins=20
)
plt.show()
```

* `bins`가 많음: 구간이 세밀해짐
* `bins`가 적음: 전체 형태가 단순해짐
* 너무 많은 `bins`는 분포를 오히려 보기 어렵게 만듦

### 상자그림

중앙값·사분위수·이상치를 확인함.

```python
sns.boxplot(
    data=gym,
    y='소모칼로리'
)
plt.show()
```

```text
상자 가운데 선 → 중앙값
상자 범위      → Q1~Q3
수염 밖 점     → 이상치 후보
```

### 밀도·바이올린

```python
sns.kdeplot(data=gym, x='소모칼로리', fill=True)

sns.violinplot(data=gym, y='소모칼로리')
```

* `kdeplot`: 부드러운 분포 곡선
* `violinplot`: 분포 모양과 요약을 함께 표시

---

## 범주형 개수

```python
sns.countplot(
    data=gym,
    x='프로그램'
)
plt.show()
```

`countplot`은 각 범주가 몇 행인지 자동으로 계산함.

```python
gym['프로그램'].value_counts()
```

위 계산을 막대그래프로 표시한 것과 비슷함.

---

# 5. 이변량 분석

변수 두 개의 차이 또는 관계를 확인함.

## 범주별 평균

```python
sns.barplot(
    data=gym,
    x='프로그램',
    y='운동시간',
    errorbar=None
)
plt.show()
```

`barplot`은 기본적으로 그룹별 평균을 계산함.

```text
x → 그룹 기준
y → 평균을 구할 수치형 값
```

단순 개수는 `countplot`, 평균 비교는 `barplot`을 사용함.

---

## 그룹별 분포 비교

```python
sns.boxplot(
    data=gym,
    x='프로그램',
    y='소모칼로리'
)
plt.show()
```

평균뿐 아니라 중앙값·분포 범위·이상치를 함께 비교함.

```python
sns.violinplot(
    data=gym,
    x='프로그램',
    y='소모칼로리'
)
```

---

## 두 수치의 관계

```python
sns.scatterplot(
    data=gym,
    x='운동시간',
    y='소모칼로리',
    hue='프로그램'
)
plt.show()
```

* 점 하나: 데이터 한 행
* 오른쪽 위 방향: 두 값이 함께 증가할 가능성
* `hue`: 범주별 색 구분
* `size`: 수치 크기로 점 크기 구분

그래프에서 관계가 보여도 바로 원인이라고 단정하면 안 됨.

---

## 시간에 따른 추이

날짜 열은 먼저 날짜 자료형으로 변환함.

```python
gym['방문일'] = pd.to_datetime(gym['방문일'])
gym['월'] = gym['방문일'].dt.month
```

월별 방문 수 계산:

```python
monthly = (
    gym.groupby('월')
       .size()
       .reset_index(name='방문수')
)
```

꺾은선 그래프:

```python
sns.lineplot(
    data=monthly,
    x='월',
    y='방문수',
    marker='o',
    errorbar=None
)
plt.show()
```

* `marker='o'`: 실제 데이터 지점 표시
* 시간 순서 데이터는 정렬된 상태인지 확인 필요

---

# 6. 다변량 분석

세 개 이상의 변수를 함께 확인함.

## 히트맵

먼저 `pivot_table`로 격자형 집계표를 만들고 색으로 표현함.

```python
pv = gym.pivot_table(
    index='요일',
    columns='시간대',
    values='운동시간',
    aggfunc='mean'
)

sns.heatmap(
    pv,
    annot=True,
    fmt='.1f'
)

plt.show()
```

* `annot=True`: 셀 안에 숫자 표시
* `fmt='.1f'`: 소수점 한 자리
* 색이 진할수록 값이 크다는 식으로 빠르게 비교 가능

```text
pivot_table → 값을 집계
heatmap     → 집계 결과를 색으로 표현
```

---

## Pairplot

여러 수치형 열의 관계를 한 번에 확인함.

```python
sns.pairplot(
    penguins,
    vars=['bill_length_mm', 'bill_depth_mm', 'body_mass_g'],
    hue='species'
)

plt.show()
```

* 대각선: 각 변수의 분포
* 나머지 칸: 두 변수의 산점도
* 열이 너무 많으면 복잡해지므로 필요한 열만 선택하는 것이 좋음
* `pairplot`은 자체 Figure를 만들므로 `plt.subplots()`가 필요 없음

---

# 7. 구성비 표현

## 파이차트

Seaborn에는 파이차트 전용 함수가 없어 Matplotlib을 사용함.

```python
counts = gym['프로그램'].value_counts()

plt.pie(
    counts.values,
    labels=counts.index,
    autopct='%1.0f%%'
)

plt.title('프로그램 구성비')
plt.show()
```

범주가 많으면 비교가 어려워져 막대그래프가 더 적합함.

## 누적 비율 막대

```python
sns.histplot(
    data=tips,
    x='day',
    hue='time',
    multiple='fill',
    stat='proportion',
    shrink=0.8
)

plt.ylabel('비율')
plt.show()
```

* `multiple='fill'`: 각 막대의 합을 1로 맞춤
* `multiple='stack'`: 실제 개수를 쌓아서 표시

---

# 8. 그래프 다듬기

## 제목과 축 이름

```python
ax.set_title('프로그램별 운동시간')
ax.set_xlabel('프로그램')
ax.set_ylabel('운동시간(분)')
```

## 범례

```python
ax.plot(x, y1, label='평균')
ax.plot(x, y2, label='최댓값')
ax.legend(title='구분')
```

`label=`로 이름을 지정하고 `legend()`로 표시함.

## 눈금 회전

```python
ax.tick_params(
    axis='x',
    rotation=45
)
```

범주 이름이 겹칠 때 사용함.

## 여백 정리

```python
fig.tight_layout()
```

여러 그래프의 제목·축이 겹치지 않게 조정함.

## 전체 제목

```python
fig.suptitle(
    '피트니스 센터 이용 현황',
    fontsize=14
)
```

## 저장

```python
fig.savefig(
    'output/dashboard.png',
    dpi=150,
    bbox_inches='tight'
)
```

* `dpi`: 이미지 해상도
* `bbox_inches='tight'`: 불필요한 바깥 여백 제거

저장은 `plt.show()`보다 먼저 하는 편이 안전함.

---

# 9. 미니 대시보드 예시

```python
fig, axes = plt.subplots(
    2, 2,
    figsize=(13, 9)
)

# 1. 운동시간 분포
sns.histplot(
    data=gym,
    x='운동시간',
    bins=20,
    ax=axes[0, 0]
)
axes[0, 0].set_title('운동시간 분포')

# 2. 프로그램별 방문 건수
sns.countplot(
    data=gym,
    x='프로그램',
    ax=axes[0, 1]
)
axes[0, 1].set_title('프로그램별 방문 건수')

# 3. 운동시간과 소모칼로리
sns.scatterplot(
    data=gym,
    x='운동시간',
    y='소모칼로리',
    hue='프로그램',
    ax=axes[1, 0]
)
axes[1, 0].set_title('운동시간과 소모칼로리')

# 4. 월별 방문 추이
sns.lineplot(
    data=monthly,
    x='월',
    y='방문수',
    marker='o',
    errorbar=None,
    ax=axes[1, 1]
)
axes[1, 1].set_title('월별 방문 추이')

fig.suptitle('피트니스 센터 이용 현황', fontsize=14)
fig.tight_layout()

plt.show()
```

## 위치 지정

```text
axes[0, 0] → 왼쪽 위
axes[0, 1] → 오른쪽 위
axes[1, 0] → 왼쪽 아래
axes[1, 1] → 오른쪽 아래
```

---

# 핵심 요약

```text
Figure       → 전체 도화지
Axes         → 개별 그래프 칸
subplots     → Figure와 Axes 생성
ax=          → 그래프를 그릴 칸 지정

histplot     → 수치 분포
countplot    → 범주별 개수
barplot      → 범주별 평균
boxplot      → 중앙값·이상치·분포 비교
scatterplot  → 두 수치의 관계
lineplot     → 시간에 따른 추이
heatmap      → 피벗 집계 결과 비교
pairplot     → 여러 수치 관계 개괄
```

```text
분포 확인 → histplot·boxplot
그룹 비교 → barplot·boxplot
관계 확인 → scatterplot
시간 추이 → lineplot
집계 격자 → pivot_table + heatmap
여러 변수 → pairplot
대시보드 → plt.subplots + ax=
```

그래프를 그린 뒤에는 **보이는 사실 → 가능한 해석 → 추가 확인할 내용** 순서로 정리해야 함.
::: 
