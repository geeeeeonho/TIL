# 기술통계·확률·상관 분석 함수 중심 정리

> 흐름: **데이터 요약 → 이상치·분포 확인 → 변수 관계 확인 → 확률분포로 확률 계산**
>
> 추론통계 내용은 제외하고 현재 강의 범위만 정리함.

---

## 0. 준비

```python
import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
from scipy import stats

mpg = pd.read_csv('data/mpg.csv')
fac = pd.read_csv('data/factory_quality.csv')
```

- `mpg`: 자동차 연비 데이터프레임
- `fac`: 공장 품질 데이터프레임
- `df['열이름']`: 데이터프레임에서 한 열을 `Series`로 선택
- 통계 계산 전에는 필요한 열의 결측치를 확인하거나 `dropna()`로 제거

---

## 1. 데이터 전체 요약

### `describe()`

| 코드 | 작동 |
|---|---|
| `df.describe()` | 수치형 열의 개수·평균·표준편차·최솟값·사분위수·최댓값 요약 |
| `df.describe(exclude='number')` | 범주형 열의 개수·고유값 수·최빈값·최빈값 빈도 요약 |
| `df.info()` | 열 이름·자료형·결측치 개수 확인 |

```python
# 수치형 열 요약
display(mpg.describe())

# 범주형 열 요약
display(mpg.describe(exclude='number'))

# 자료형과 결측치 확인
mpg.info()
```

> 코드 맨 앞의 불필요한 공백은 제거해서 사용.

---

## 2. 대표값: 데이터의 중심

### 핵심 함수

| 함수 | 작동 | 특징 |
|---|---|---|
| `x.mean()` | 산술평균 | 이상치에 민감 |
| `x.median()` | 정렬했을 때 가운데 값 | 이상치에 비교적 강함 |
| `x.mode()` | 가장 자주 나온 값 | 여러 개일 수 있어 `Series`로 반환 |
| `stats.trim_mean(x, 0.1)` | 양쪽에서 각각 10% 제거 후 평균 | 극단값 영향 완화 |
| `np.average(x, weights=w)` | 값마다 가중치를 반영한 평균 | 중요도·그룹 크기 반영 |

### 핵심 예시

```python
x = mpg['mpg'].dropna()  # 결측치를 제외한 연비 값

print('평균:', round(x.mean(), 3))
print('중앙값:', round(x.median(), 3))
print('최빈값:', x.mode().tolist())  # 최빈값이 여러 개일 수 있으므로 목록 변환
print('절사평균:', round(stats.trim_mean(x, 0.1), 3))  # 하위·상위 10%씩 제거
```

#### 가중평균

그룹마다 데이터 수가 다르면 그룹 평균을 다시 단순 평균 내지 말고 그룹 크기를 가중치로 사용.

```python
origin_stats = (
    mpg.groupby('origin')['mpg']
       .agg(mean='mean', count='count')
)

weighted_mean = np.average(
    origin_stats['mean'],
    weights=origin_stats['count']  # 제조국별 실제 차량 수를 가중치로 사용
)

print('가중평균:', round(weighted_mean, 3))
print('전체 평균:', round(mpg['mpg'].mean(), 3))
```

---

## 3. 산포도: 데이터의 퍼짐

### 핵심 함수

| 지표 | 코드 | 작동 |
|---|---|---|
| 범위 | `x.max() - x.min()` | 최댓값과 최솟값 사이의 전체 폭 |
| IQR | `stats.iqr(x)` | `Q3 - Q1`, 가운데 50%의 폭 |
| 표본분산 | `x.var(ddof=1)` | 편차 제곱합을 `n-1`로 나눔 |
| 모분산 | `x.var(ddof=0)` | 편차 제곱합을 `n`으로 나눔 |
| 표본표준편차 | `x.std(ddof=1)` | 표본분산의 제곱근 |
| 변동계수 | `표준편차 / 평균 × 100` | 단위를 제거한 상대적 퍼짐 |

- `ddof`: 분모에서 빼는 자유도 수
- `ddof=1`: 표본 통계에서 주로 사용
- `ddof=0`: 현재 값 전체를 모집단으로 볼 때 사용

### 핵심 예시

```python
x = mpg['mpg'].dropna()

print('범위:', round(x.max() - x.min(), 3))
print('IQR:', round(stats.iqr(x), 3))
print('표본분산:', round(x.var(ddof=1), 3))
print('모분산:', round(x.var(ddof=0), 3))
print('표본표준편차:', round(x.std(ddof=1), 3))
```

#### 변동계수 비교

```python
for col in ['mpg', 'weight', 'acceleration']:
    values = mpg[col].dropna()
    cv = values.std(ddof=1) / values.mean() * 100
    print(f'{col:13s}: CV = {cv:.1f}%')
```

> CV는 **단위나 평균 크기가 다른 양수형 변수**의 상대적 퍼짐을 비교할 때 사용. 평균이 0에 가깝거나 음수인 변수에는 해석이 부적절할 수 있음.

---

## 4. 이상치 확인

이상치 규칙은 값을 자동 삭제하는 기준이 아니라 **확인할 후보를 찾는 기준**.

## 4.1 IQR 1.5배 규칙

```python
x = mpg['mpg'].dropna()

q1 = x.quantile(0.25)  # 25% 지점
q3 = x.quantile(0.75)  # 75% 지점
iqr = q3 - q1

lower = q1 - 1.5 * iqr
upper = q3 + 1.5 * iqr

# 원본 데이터프레임에서 IQR 경계 밖의 행 선택
outliers = mpg[(mpg['mpg'] < lower) | (mpg['mpg'] > upper)]

# 경계 안의 행만 선택
cleaned = mpg[(mpg['mpg'] >= lower) & (mpg['mpg'] <= upper)]

print(f'IQR 경계: {lower:.2f} ~ {upper:.2f}')
print('이상치 후보 수:', len(outliers))
```

- `quantile(0.25)`: 1사분위수 `Q1`
- `quantile(0.75)`: 3사분위수 `Q3`
- 판다스 조건을 합칠 때 `and`, `or`가 아니라 `&`, `|` 사용
- 각 조건은 반드시 괄호로 묶음

#### 상자그림

```python
fig, ax = plt.subplots(figsize=(8, 4))
sns.boxplot(x=mpg['mpg'], ax=ax)
ax.set_title('연비 상자그림')
plt.show()
```

## 4.2 Z-score

```python
x = mpg['mpg']

z_scores = (x - x.mean()) / x.std(ddof=1)

z_outliers = mpg[z_scores.abs() > 3]   # 평균에서 표준편차 3칸 초과
z_cleaned = mpg[z_scores.abs() <= 3]   # |Z|가 3 이하인 행

print('|Z| > 3 이상치 후보 수:', len(z_outliers))
```

- `abs()` 또는 `.abs()`: 음수를 양수로 바꿔 거리만 비교
- 보통 `|Z| > 3`을 후보 기준으로 사용
- `2`를 기준으로 쓸 수도 있지만 더 많은 값이 제외됨

---

## 5. 분포 모양: 왜도와 첨도

### 핵심 함수

| 함수 | 작동 |
|---|---|
| `stats.skew(x)` | 좌우 치우침 측정 |
| `stats.kurtosis(x)` | 정규분포 대비 꼬리·극단값 정도 측정 |
| `sns.histplot()` | 구간별 데이터 빈도 또는 밀도 시각화 |

#### 해석

- 왜도 `> 0`: 오른쪽 꼬리가 김
- 왜도 `< 0`: 왼쪽 꼬리가 김
- 왜도 `≈ 0`: 좌우가 비교적 대칭
- 첨도 `> 0`: 정규분포보다 꼬리가 두껍고 극단값이 나타날 가능성이 큼
- 첨도 `< 0`: 정규분포보다 꼬리가 얇음
- SciPy의 기본 `stats.kurtosis()`는 정규분포를 `0`으로 보는 **초과첨도** 반환

### 핵심 예시

```python
alarms = fac['설비알람수'].dropna()

fig, ax = plt.subplots(figsize=(7, 4))
sns.histplot(x=alarms, discrete=True, ax=ax)  # 정수형 횟수라 discrete=True
ax.set_title('배치별 설비 알람 횟수')
plt.show()

print('왜도:', round(stats.skew(alarms), 3))
print('첨도:', round(stats.kurtosis(alarms), 3))
```

---

## 6. 상관분석

상관계수는 두 변수가 함께 움직이는 방향과 세기를 요약.

### 피어슨과 스피어만

| 함수 | 측정 대상 | 특징 |
|---|---|---|
| `stats.pearsonr(x, y)` | 직선 관계 | 이상치에 민감 |
| `stats.spearmanr(x, y)` | 순위 기반 단조 관계 | 이상치에 비교적 강함 |
| `df.corr()` | 수치형 열 전체의 피어슨 상관행렬 | 모든 변수 쌍 계산 |
| `df.corr(method='spearman')` | 수치형 열 전체의 스피어만 상관행렬 | 순위 기반 계산 |

- 계수 범위: `-1 ~ 1`
- 양수: 한쪽이 커질수록 다른 쪽도 커지는 경향
- 음수: 한쪽이 커질수록 다른 쪽은 작아지는 경향
- `0`에 가까움: 해당 방식의 관계가 약함

### 핵심 예시

```python
# 두 열 중 하나라도 결측치인 행 제거
pair = mpg[['weight', 'mpg']].dropna()

pearson_r = stats.pearsonr(pair['weight'], pair['mpg']).statistic
spearman_rho = stats.spearmanr(pair['weight'], pair['mpg']).statistic

print('피어슨:', round(pearson_r, 3))
print('스피어만:', round(spearman_rho, 3))
```

> 일부 SciPy 버전에서는 결과의 첫 값을 `[0]`으로 꺼낼 수도 있음. `.statistic`이 의미를 더 명확히 보여 줌.

#### 산점도

```python
fig, ax = plt.subplots(figsize=(7, 5))
sns.scatterplot(data=pair, x='weight', y='mpg', ax=ax)
ax.set_title('차량 무게와 연비')
plt.show()
```

#### 상관행렬과 히트맵

```python
corr = mpg.corr(numeric_only=True)

display(corr['mpg'].round(3).sort_values())

plt.figure(figsize=(8, 6))
sns.heatmap(
    corr,
    annot=True,       # 각 칸에 숫자 표시
    cmap='coolwarm',  # 음수와 양수를 다른 색으로 표시
    fmt='.2f',
    vmin=-1,
    vmax=1
)
plt.title('수치형 변수 상관행렬')
plt.show()
```

### 반드시 주의할 점

- 피어슨은 U자형 같은 비선형 관계를 놓칠 수 있음
- 스피어만도 방향이 계속 바뀌는 비단조 관계는 잘 잡지 못함
- 이상치 하나가 상관계수를 크게 바꿀 수 있음
- 데이터 범위를 일부만 사용하면 상관이 약해질 수 있음
- **상관관계는 인과관계가 아님**
- 따라서 상관계수와 산점도를 함께 확인

---

## 7. 데이터에서 확률 구하기

판다스에서 불리언 값은 계산할 때 `True=1`, `False=0`으로 취급됨.
따라서 `(조건).mean()`은 조건을 만족한 데이터의 비율.

```python
d = fac['불량수'].dropna()

p_ok = (d <= 2).mean()               # 불량 2개 이하
p_recheck = d.between(3, 5).mean()   # 3~5개, 양끝 포함
p_discard = (d >= 6).mean()          # 6개 이상

print('정상 출하 확률:', p_ok)
print('재검사 확률:', p_recheck)
print('폐기 확률:', p_discard)
print('합계:', p_ok + p_recheck + p_discard)
```

- `Series.between(a, b)`: `a <= 값 <= b`
- 세 조건이 빠짐없이 겹치지 않게 나뉘면 확률 합은 `1`

### 기대비용

```python
expected_cost = (
    0 * p_ok
    + 500_000 * p_recheck
    + 3_000_000 * p_discard
)

print(f'배치당 기대 처리비용: {expected_cost:,.0f}원')
print(f'480개 배치 기대비용: {480 * expected_cost:,.0f}원')
```

기대값은 가능한 값에 각각의 확률을 곱해 더한 값.

---

## 8. 기대값과 분산 직접 계산

```python
outcomes = np.array([1, 2, 3, 4, 5, 6])  # 주사위가 가질 값
probs = np.ones(6) / 6                    # 각 값의 확률은 1/6

expected = np.sum(outcomes * probs)
variance = np.sum((outcomes - expected) ** 2 * probs)
std = np.sqrt(variance)

print('기대값:', expected)
print('분산:', round(variance, 3))
print('표준편차:', round(std, 3))
```

| 함수 | 작동 |
|---|---|
| `np.array()` | 값들을 NumPy 배열로 생성 |
| `np.ones(n)` | 값이 모두 1인 길이 `n` 배열 생성 |
| `np.sum()` | 배열 값 합계 |
| `np.sqrt()` | 제곱근 계산 |

---

## 9. 확률분포 공통 메서드

| 메서드 | 뜻 |
|---|---|
| `dist.pmf(k)` | 이산형에서 `P(X = k)` |
| `dist.pdf(x)` | 연속형의 확률밀도. 높이 자체가 확률은 아님 |
| `dist.cdf(x)` | `P(X <= x)` 누적확률 |
| `dist.sf(x)` | `P(X > x)`, `1 - cdf(x)` |
| `dist.ppf(q)` | 누적확률 `q`에 해당하는 경곗값 |
| `dist.mean()` | 분포의 기대값 |
| `dist.var()` | 분포의 분산 |
| `dist.median()` | 분포의 중앙값 |

### 이산형 경계

| 구하려는 확률 | 코드 |
|---|---|
| `P(X = k)` | `dist.pmf(k)` |
| `P(X <= k)` | `dist.cdf(k)` |
| `P(X < k)` | `dist.cdf(k - 1)` |
| `P(X >= k)` | `dist.sf(k - 1)` |
| `P(X > k)` | `dist.sf(k)` |
| `P(a <= X <= b)` | `dist.cdf(b) - dist.cdf(a - 1)` |

> 이산형에서 `sf(k)`는 `k 초과`. `k 이상`은 `sf(k - 1)`.

### 연속형 경계

연속형은 한 점의 확률이 0이므로 이하와 미만, 이상과 초과의 확률이 같음.

| 구하려는 확률 | 코드 |
|---|---|
| `P(X <= x)` | `dist.cdf(x)` |
| `P(X >= x)` | `dist.sf(x)` |
| `P(a <= X <= b)` | `dist.cdf(b) - dist.cdf(a)` |
| 하위 `p` 경계 | `dist.ppf(p)` |
| 상위 `p` 경계 | `dist.ppf(1 - p)` |

```python
norm = stats.norm(loc=5, scale=2)

print('하위 10% 경계:', norm.ppf(0.10))
print('상위 10% 커트라인:', norm.ppf(0.90))  # 위쪽에 10%가 남는 경계
```

---

## 10. 주요 확률분포

## 10.1 이산형

| 분포 | 생성 | 사용 상황 |
|---|---|---|
| 베르누이 | `stats.bernoulli(p)` | 성공·실패 한 번 |
| 이항 | `stats.binom(n, p)` | 독립 시행 `n`번 중 성공 횟수 |
| 포아송 | `stats.poisson(mu=lam)` | 일정 시간·공간의 사건 횟수 |

#### 베르누이

```python
bern = stats.bernoulli(p=0.3)  # 성공확률 30%

print('성공 확률:', bern.pmf(1))
print('실패 확률:', bern.pmf(0))
print('기대값:', bern.mean())
print('분산:', bern.var())
```

#### 이항

```python
binom = stats.binom(n=20, p=0.05)  # 20개 중 불량 수, 불량률 5%

print('불량 0개:', round(binom.pmf(0), 4))
print('불량 2개 이하:', round(binom.cdf(2), 4))
print('불량 3개 이상:', round(binom.sf(2), 4))  # P(X >= 3) = P(X > 2)
```

#### 포아송

```python
poisson = stats.poisson(mu=3)  # 시간당 평균 사건 수 3회

print('정확히 5회:', round(poisson.pmf(5), 4))
print('2회 이하:', round(poisson.cdf(2), 4))
print('기대값:', poisson.mean())
print('분산:', poisson.var())
```

## 10.2 연속형

| 분포 | 생성 | 사용 상황 |
|---|---|---|
| 균등 | `stats.uniform(loc=a, scale=b-a)` | 구간 `[a, b]`에서 고르게 발생 |
| 지수 | `stats.expon(scale=평균대기시간)` | 다음 사건까지의 대기시간 |
| 정규 | `stats.norm(loc=평균, scale=표준편차)` | 평균 주변에 대칭인 연속형 값 |

#### 균등분포

```python
uniform = stats.uniform(loc=0, scale=10)  # 0~10분

print('평균 대기시간:', uniform.mean())
print('3분 이내 도착 확률:', uniform.cdf(3))
```

#### 지수분포

```python
expon = stats.expon(scale=1000)  # 평균 수명 1000시간

print('500시간 이내 고장 확률:', round(expon.cdf(500), 4))
print('중앙 수명:', round(expon.median(), 1))
```

#### 정규분포

```python
normal = stats.norm(loc=70, scale=10)  # 평균 70, 표준편차 10

print('80점 이상:', round(normal.sf(80), 4))
print('60점 이하:', round(normal.cdf(60), 4))
print('상위 10% 커트라인:', round(normal.ppf(0.90), 1))
```

---

## 11. Z-score와 정규분포

Z-score는 값이 평균에서 표준편차 몇 칸 떨어졌는지 나타냄.

```python
x = mpg['mpg'].dropna()
z = (x - x.mean()) / x.std(ddof=1)

print(z.head())
```

SciPy 함수 사용:

```python
z = stats.zscore(x, ddof=1)
```

- `Z = 0`: 평균과 같음
- `Z = 2`: 평균보다 표준편차 2개만큼 큼
- `Z = -1`: 평균보다 표준편차 1개만큼 작음

정규분포에서는 대략:

- 평균 ± `1σ`: 68%
- 평균 ± `2σ`: 95%
- 평균 ± `3σ`: 99.7%

---

## 12. 정규성 확인

정규분포를 이용해 확률을 계산하기 전에 분포가 정규분포와 비슷한지 확인.
하나의 지표만으로 단정하지 않고 다음을 함께 봄.

1. 히스토그램과 정규곡선
2. Q-Q Plot
3. 왜도·첨도
4. 평균 ±1σ·±2σ 안의 실제 비율

### 핵심 예시

```python
s = mpg['acceleration'].dropna()
mean = s.mean()
std = s.std(ddof=1)

fig, axes = plt.subplots(1, 2, figsize=(12, 4))

# 히스토그램을 밀도로 표시
sns.histplot(s, bins=25, stat='density', ax=axes[0])

# 같은 평균·표준편차를 가진 정규곡선 겹치기
xs = np.linspace(s.min(), s.max(), 200)
axes[0].plot(xs, stats.norm.pdf(xs, loc=mean, scale=std))
axes[0].set_title('히스토그램과 정규곡선')

# 점이 직선에 가까울수록 정규분포와 비슷함
stats.probplot(s, dist='norm', plot=axes[1])
axes[1].set_title('Q-Q Plot')

plt.tight_layout()
plt.show()

within_1 = ((s - mean).abs() <= std).mean()
within_2 = ((s - mean).abs() <= 2 * std).mean()

print('왜도:', round(stats.skew(s), 3))
print('첨도:', round(stats.kurtosis(s), 3))
print('평균 ±1σ 비율:', f'{within_1:.1%}')
print('평균 ±2σ 비율:', f'{within_2:.1%}')
```

#### 핵심 함수 작동

| 함수 | 작동 |
|---|---|
| `np.linspace(a, b, n)` | `a`부터 `b`까지 일정 간격 값 `n`개 생성 |
| `stats.norm.pdf(xs, loc, scale)` | 지정한 정규분포의 각 `x` 위치 밀도 계산 |
| `stats.probplot(s, dist='norm', plot=ax)` | 데이터와 정규분포 분위수를 비교하는 Q-Q Plot 생성 |
| `stat='density'` | 히스토그램 면적 합을 1로 맞춰 PDF와 비교 가능하게 함 |

> Q-Q Plot의 상관값이 높다는 사실만으로 정규성을 확정하지 않음. 점의 휘어짐과 꼬리 부분도 직접 확인.

---

## 13. 품질 데이터 분석 흐름

```python
# 1) 라인별 내용량 요약
line_fill = (
    fac.groupby('라인')['내용량_ml']
       .agg(['mean', 'median', 'std'])
       .round(2)
)
display(line_fill)

# 2) 라인별 평균 불량수
line_defects = (
    fac.groupby('라인')['불량수']
       .mean()
       .sort_values(ascending=False)
       .round(2)
)
display(line_defects)

# 3) 불량수와 라인속도의 상관
pair = fac[['불량수', '라인속도_bpm']].dropna()
r = stats.pearsonr(pair['불량수'], pair['라인속도_bpm']).statistic
print('피어슨 상관:', round(r, 3))

# 4) 내용량이 규격 495~505ml를 벗어날 이론적 확률
fill = fac['내용량_ml'].dropna()
normal = stats.norm(loc=fill.mean(), scale=fill.std(ddof=1))
expected_out_rate = normal.cdf(495) + normal.sf(505)

# 실제 데이터의 이탈 비율
actual_out_rate = ((fill < 495) | (fill > 505)).mean()

print('정규분포 기반 이탈률:', f'{expected_out_rate:.1%}')
print('실제 이탈률:', f'{actual_out_rate:.1%}')
```

### 결과 해석 순서

1. 평균·중앙값으로 중심 확인
2. 표준편차·IQR로 퍼짐 확인
3. 이상치와 왜도·첨도 확인
4. 정규성 확인 후 이론 확률 계산
5. 실제 비율과 이론 확률 비교
6. 상관계수와 산점도로 변수 관계 확인
7. 원인이라고 단정하지 말고 추가 조사·실험 진행

> 예: 라인속도와 불량수에 양의 상관이 있어도, 즉시 속도를 낮춰야 한다고 단정할 수 없음. 라인·설비·작업자·제품 종류 등 다른 요인을 함께 확인해야 함.

---

## 14. 핵심 함수 총정리

| 목적 | 함수·메서드 |
|---|---|
| 전체 요약 | `describe`, `info` |
| 대표값 | `mean`, `median`, `mode`, `stats.trim_mean`, `np.average` |
| 산포도 | `max`, `min`, `quantile`, `stats.iqr`, `var`, `std` |
| 이상치 | `quantile`, 불리언 조건, `abs` |
| 분포 모양 | `stats.skew`, `stats.kurtosis`, `sns.histplot` |
| 상관분석 | `stats.pearsonr`, `stats.spearmanr`, `corr`, `sns.scatterplot`, `sns.heatmap` |
| 데이터 기반 확률 | `(조건).mean()`, `between` |
| 배열 계산 | `np.array`, `np.ones`, `np.sum`, `np.sqrt`, `np.linspace` |
| 이산분포 | `stats.bernoulli`, `stats.binom`, `stats.poisson`, `pmf` |
| 연속분포 | `stats.uniform`, `stats.expon`, `stats.norm`, `pdf` |
| 확률·경곗값 | `cdf`, `sf`, `ppf` |
| 표준화·정규성 | `stats.zscore`, `stats.probplot` |
| 그룹 요약 | `groupby`, `agg`, `sort_values` |

---

## 15. 자주 틀리는 부분

```python
# 잘못된 상위 10% 경계
normal.ppf(0.10)   # 하위 10% 경계

# 올바른 상위 10% 커트라인
normal.ppf(0.90)
```

```python
# 이산형 X에서 5 이상
binom.sf(4)  # P(X >= 5)

# 이산형 X에서 5 초과
binom.sf(5)  # P(X > 5)
```

```python
# 두 조건 결합
mask = (x >= lower) & (x <= upper)  # 괄호 필요
```

```python
# 결측치가 있을 수 있는 두 열의 상관
pair = df[['x', 'y']].dropna()
stats.pearsonr(pair['x'], pair['y'])
```

```python
# SciPy 왜도·첨도 계산 전 결측치 제거
s = df['열'].dropna()
stats.skew(s)
stats.kurtosis(s)
```

---

### 최종 압축

- 중심: `mean`, `median`, `mode`, `trim_mean`
- 퍼짐: `std(ddof=1)`, `IQR`, 필요할 때 `CV`
- 이상치: IQR 규칙 또는 Z-score로 후보 확인
- 모양: `skew`, `kurtosis`, 히스토그램
- 관계: `pearsonr`·`spearmanr`와 산점도를 함께 확인
- 확률: 이산형은 `pmf`, 연속형은 `pdf`의 구간 넓이
- 누적·초과·경계: `cdf`, `sf`, `ppf`
- 정규분포 적용 전: 히스토그램·Q-Q Plot·왜도·첨도 확인
- 상관관계만으로 원인이나 조치를 단정하지 않음
