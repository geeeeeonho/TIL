# 통계 검정과 회귀 총정리

> 목표: **무엇을 비교하는지 → 어떤 검정을 쓰는지 → 결과를 어떻게 읽는지** 한 번에 판단하기

---

## 0. 전체 분석 흐름

```text
1. 질문 확인
   ├─ 평균·수치 비교인가?
   ├─ 범주형 빈도 비교인가?
   └─ 변수 사이 관계·예측인가?

2. 자료 구조 확인
   ├─ 같은 대상의 전·후인가?
   ├─ 서로 다른 독립 집단인가?
   └─ 집단이 2개인가, 3개 이상인가?

3. 가정 점검
   ├─ 독립성
   ├─ 정규성
   └─ 등분산성

4. 검정 선택
   ├─ 모수 검정
   ├─ Welch 보정
   └─ 비모수 검정

5. 결과 해석
   ├─ p-value: 우연이라고 보기 어려운가?
   ├─ 효과크기: 실제 차이가 얼마나 큰가?
   └─ 사후검정: 구체적으로 어느 집단이 다른가?
```

---

# 1. 가설검정 기본

## 1.1 귀무가설과 대립가설

- **귀무가설 `H₀`**: 대립가설 반대(차이·효과·관계가 없음)
- **대립가설 `H₁`**: 주장하고 싶은 바 (차이·효과·관계가 있음)

예:

```text
H₀: 두 집단의 평균은 같다.
H₁: 두 집단의 평균은 다르다.
```

## 1.2 p-value

**`H₀`가 참이라고 가정했을 때, 현재 관측값만큼 또는 그보다 더 극단적인 결과가 나올 확률**

```text
p-value < α   → H₀ 기각 → 통계적으로 유의
p-value ≥ α   → H₀ 기각 못 함 → 차이가 없다는 증명이 아니라 증거 부족
```

보통 유의수준은 다음처럼 사용.

```python
alpha = 0.05
```

> p-value는 `H₀가 틀릴 확률`이나 `결과가 우연일 확률`이 아님.

## 1.3 단측검정과 양측검정

| 검정 | 대립가설 | 사용 상황 |
|---|---|---|
| 양측검정 | `H₁: 다르다(≠)` | 방향을 미리 확신할 수 없을 때 |
| 단측검정 | `H₁: 더 크다(>)` 또는 `더 작다(<)` | 분석 전에 방향을 명확히 정했을 때 |

결과를 본 뒤 유리한 방향으로 단측검정을 선택하면 안 됨.

## 1.4 1종 오류·2종 오류·검정력

| 실제 상태 | `H₀` 기각 | `H₀` 기각 못 함 |
|---|---|---|
| 실제 차이 없음 | **1종 오류 `α`**: 없는 차이를 있다고 판단 | 옳은 판단 |
| 실제 차이 있음 | **검정력 `1-β`**: 실제 차이를 발견 | **2종 오류 `β`**: 실제 차이를 놓침 |

- `α`를 지나치게 낮추면 1종 오류는 줄지만 2종 오류가 커질 수 있음
- **검정력**: 실제 차이가 있을 때 이를 발견할 확률

---

# 2. 검정 전 가정 점검

## 2.1 독립성

관측값끼리 서로 영향을 주지 않아야 함.

```text
서로 다른 사람·대상 → 독립표본
같은 사람의 전·후 측정 → 대응표본
```

독립성은 검정 결과보다 **자료를 어떻게 수집했는지**로 판단.

## 2.2 정규성

데이터가 정규분포에 가까운지 확인.

```python
import pingouin as pg

pg.normality(x)
```

`pg.normality()`는 Shapiro-Wilk 검정 결과를 제공.

- `H₀`: 정규분포를 따른다
- `pval > 0.05` → 정규성을 기각하지 못함
- `pval < 0.05` → 정규성 위배 가능성

### 상황별 확인 대상

| 검정 | 정규성을 확인할 값 |
|---|---|
| 1표본 t | 한 집단의 값 |
| 대응표본 t | `후 - 전` 차이값 |
| 독립 2표본 t | 각 집단을 따로 |
| ANOVA | 각 집단 또는 모형의 잔차 |

### Q-Q Plot도 함께 확인

```text
점이 기준 직선에 가까움 → 정규분포에 가까움
꼬리 부분이 크게 휨 → 이상치·비정규 가능성
```

Shapiro-Wilk는 표본이 크면 작은 이탈에도 민감하고, 표본이 작으면 위배를 잘 발견하지 못할 수 있음.  
따라서 **p-value만 보지 말고 Q-Q Plot·표본 크기·이상치를 함께 판단**.

## 2.3 등분산성

독립 집단들의 퍼진 정도가 비슷한지 확인.

```python
pg.homoscedasticity(
    data=df,
    dv='값열',
    group='집단열'
)
```

- `H₀`: 집단들의 분산이 같다
- `pval > 0.05` 또는 `equal_var=True` → 등분산으로 봐도 무방
- 등분산 위배 → Student 방식 대신 Welch 방식

> 등분산은 주로 **서로 다른 독립 집단 비교**에서 확인.  
> 1표본·대응표본 검정에는 집단 간 등분산 조건이 없음.

---

# 3. 검정 선택 한눈에 보기

## 3.1 수치형 자료
- 검정방법 : 
```text
한 집단 vs 기준값
├─ 정규성 만족 → 1표본 t
└─ 정규성 크게 위배 → Wilcoxon 부호순위

같은 대상의 전·후
├─ 차이값 정규성 만족 → 대응표본 t
└─ 차이값 정규성 크게 위배 → Wilcoxon 부호순위

독립 두 집단
├─ 정규성 만족
│  ├─ 등분산 → Student t
│  └─ 이분산 → Welch t
└─ 정규성 크게 위배 → Mann-Whitney U

독립 세 집단 이상
├─ 정규성 만족
│  ├─ 등분산 → 일반 ANOVA + Tukey
│  └─ 이분산 → Welch ANOVA + Games-Howell
└─ 정규성 크게 위배 → Kruskal-Wallis + Dunn
```

## 3.2 범주형 자료

```text
범주형 변수 1개
→ 카이제곱 적합도 검정

범주형 변수 2개
→ 카이제곱 독립성 검정

작은 2×2 교차표 + 기대빈도 부족
→ Fisher 정확검정
```

---

# 4. t-검정

t-검정은 평균 차이를 표준오차로 나눈 **t값**을 사용해 평균 차이를 판단.

```text
t값이 0에서 멀수록
→ 귀무가설 아래에서 보기 드문 결과
→ p-value가 작아질 가능성이 큼
```

## 4.1 1표본 t-검정

한 집단의 평균이 기준값과 다른지 확인.

```python
pg.ttest(x, 기준값)
```

```text
H₀: 집단 평균 = 기준값
H₁: 집단 평균 ≠ 기준값
```

## 4.2 대응표본 t-검정

같은 대상의 전·후 평균 차이를 확인.

```python
pg.ttest(after, before, paired=True)
```

핵심은 두 집단을 각각 보는 것이 아니라 다음 **차이값**을 보는 것.

```python
difference = after - before
```

## 4.3 독립 2표본 t-검정

서로 다른 두 집단의 평균을 비교.

```python
# Student t: 등분산 가정
pg.ttest(a, b, correction=False)

# Welch t: 등분산을 가정하지 않음
pg.ttest(a, b, correction=True)
```

실무에서는 등분산 여부와 관계없이 Welch t를 기본으로 사용하기도 함.

## 4.4 효과크기: Cohen's d와 Hedges' g

p-value는 **차이가 통계적으로 유의한지**, 효과크기는 **차이가 실제로 얼마나 큰지** 설명.

| `|d|` 또는 `|g|` | 일반적 해석 |
|---:|---|
| 0.2 미만 | 매우 작음 |
| 0.2~0.5 | 작음 |
| 0.5~0.8 | 중간 |
| 0.8 이상 | 큼 |

- **Cohen's d**: 평균 차이를 표준편차 단위로 표시
- **Hedges' g**: Cohen's d의 소표본 편향을 보정

예:

```text
p = 0.001, d = 0.05
→ 통계적으로 유의하지만 실제 차이는 매우 작음
```

대표본에서는 사소한 차이도 p-value가 작아질 수 있으므로 효과크기를 함께 확인.

---

# 5. 세 집단 이상: ANOVA

## 5.1 일반 ANOVA

세 집단 이상의 평균을 한 번에 비교.

```python
pg.anova(
    data=df,
    dv='값열',
    between='집단열'
)
```

```text
H₀: 모든 집단의 평균이 같다
H₁: 적어도 한 집단의 평균이 다르다
```

### F통계량

```text
F = 집단 간 변동 ÷ 집단 내 변동
```

- `F ≈ 1` → 집단 차이가 잡음과 비슷함
- F가 큼 → 집단 간 차이가 상대적으로 뚜렷함

최종 판단은 F값만 보지 않고 p-value와 함께 확인.

## 5.2 ANOVA 결과 읽기

주요 열:

| 열 | 의미 |
|---|---|
| `F` | 집단 간 차이 대비 집단 내 변동 |
| `p_unc` | 보정되지 않은 p-value |
| `np2` | 부분 에타제곱 `partial η²` |

> Pingouin의 `np2`는 일반 η²가 아니라 **부분 에타제곱**.  
> 일원 ANOVA에서는 η²와 부분 η²가 같아질 수 있지만 명칭은 구분해서 사용.

## 5.3 Tukey 사후검정

일반 ANOVA가 유의해도 **어느 집단이 다른지**는 알 수 없음.

```python
pg.pairwise_tukey(
    data=df,
    dv='값열',
    between='집단열'
)
```

| 열 | 의미 |
|---|---|
| `A`, `B` | 비교한 집단 |
| `p_tukey` | 다중비교가 보정된 p-value |
| `diff` | 두 집단 평균 차이 |
| `hedges` | 쌍별 효과크기 Hedges' g |

## 5.4 Welch ANOVA

정규성은 크게 문제없지만 등분산성이 깨진 세 집단 이상 비교.

```python
pg.welch_anova(
    data=df,
    dv='값열',
    between='집단열'
)
```

Welch ANOVA가 유의하면 Tukey 대신 **Games-Howell 사후검정** 사용.

```python
pg.pairwise_gameshowell(
    data=df,
    dv='값열',
    between='집단열'
)
```

## 5.5 효과크기

| 효과크기 | 일반적 해석 |
|---:|---|
| 0.01 미만 | 매우 작음 |
| 0.01~0.06 | 작음 |
| 0.06~0.14 | 중간 |
| 0.14 이상 | 큼 |

해석 기준은 분야에 따라 달라질 수 있음.

---

# 6. 비모수 검정

비모수 검정은 정규분포를 강하게 가정하지 않고, 주로 **순위**를 사용해 차이를 비교.

- 이상치와 비정규분포에 비교적 강함
- 모수 검정보다 검정력이 낮을 수 있음
- 단순히 평균 대신 중앙값만 비교한다고 이해하면 부정확할 수 있음

## 6.1 Wilcoxon 부호순위 검정

### 한 집단과 기준값

```python
pg.wilcoxon(x - 기준값)
```

기본값은 양측검정.

```python
# 기준값보다 큰 경향
pg.wilcoxon(x - 기준값, alternative='greater')

# 기준값보다 작은 경향
pg.wilcoxon(x - 기준값, alternative='less')
```

### 대응표본 전·후

```python
pg.wilcoxon(after, before)
```

대응쌍 차이의 부호와 순위를 함께 사용.

## 6.2 Mann-Whitney U 검정

서로 다른 두 집단의 값을 함께 순위화해 비교.

```python
pg.mwu(a, b)
```

기본적으로 묻는 질문:

```text
한 집단에서 뽑은 값이 다른 집단에서 뽑은 값보다
더 클 가능성이 서로 같은가?
```

> 두 집단의 분포 모양과 퍼짐이 비슷할 때는 중앙값 차이로 해석하기 쉬움.  
> 분포 모양이 다르면 단순한 중앙값 검정으로 해석하면 안 됨.

## 6.3 Kruskal-Wallis 검정

독립된 세 집단 이상의 순위를 한 번에 비교.

```python
pg.kruskal(
    data=df,
    dv='값열',
    between='집단열'
)
```

유의한 경우:

```text
적어도 한 집단의 분포 위치가 다름
```

어느 집단이 다른지는 **Dunn 사후검정**으로 확인.

## 6.4 순위 효과크기 RBC

**RBC(rank-biserial correlation)**는 순위 기반 효과크기.

```text
RBC = 한쪽이 이길 확률 - 질 확률
```

- 양수 → 첫 번째 집단이 더 큰 경향
- 음수 → 두 번째 집단이 더 큰 경향
- 절댓값이 클수록 차이가 큼

| `|RBC|` | 일반적 해석 |
|---:|---|
| 0.1 미만 | 매우 작음 |
| 0.1~0.3 | 작음 |
| 0.3~0.5 | 중간 |
| 0.5 이상 | 큼 |

---

# 7. 범주형 자료: 카이제곱 검정

범주형 자료는 평균이 아니라 **빈도·개수**를 비교.

## 7.1 적합도 검정

한 범주의 관측 분포가 기대 분포와 같은지 확인.

```python
from scipy.stats import chisquare

chisquare(
    f_obs=관측빈도,
    f_exp=기대빈도
)
```

예:

```text
요일별 주문량이 균등한가?
제품 선호 비율이 알려진 비율과 같은가?
```

## 7.2 독립성 검정

두 범주형 변수가 서로 관련 있는지 확인.

```python
pg.chi2_independence(
    data=df,
    x='범주1',
    y='범주2'
)
```

```text
H₀: 두 범주형 변수는 독립이다
H₁: 두 범주형 변수는 관련 있다
```

### 기대빈도

```text
기대빈도 = (행 합계 × 열 합계) ÷ 전체 표본 수
```

관측빈도와 기대빈도의 차이가 클수록 χ²가 커짐.

## 7.3 신뢰 조건

일반적인 확인 기준:

```text
1. 모든 칸의 기대빈도가 1 이상
2. 기대빈도 5 미만인 칸이 전체의 20% 이하
```

작은 `2×2` 교차표에서 기대빈도가 부족하면 **Fisher 정확검정** 고려.

## 7.4 효과크기

| 검정 | 효과크기 |
|---|---|
| 적합도 검정 | Cohen's w |
| 독립성 검정 | Cramér's V |

일반적 해석:

| 효과크기 | 해석 |
|---:|---|
| 0.1 미만 | 매우 작음 |
| 0.1~0.3 | 작음 |
| 0.3~0.5 | 중간 |
| 0.5 이상 | 큼 |

## 7.5 어느 칸이 다른가: 조정잔차

카이제곱 독립성 검정이 유의해도 어느 칸이 관계를 만드는지는 알 수 없음.

```text
|조정잔차| > 2
→ 기대보다 눈에 띄게 많거나 적은 칸
```

- 양수 → 기대보다 관측빈도가 많음
- 음수 → 기대보다 관측빈도가 적음

```text
ANOVA 유의 → 사후검정으로 집단 쌍 확인
카이제곱 유의 → 조정잔차로 교차표의 칸 확인
```

---

# 8. 상관과 회귀

## 8.1 상관

두 수치형 변수가 함께 움직이는 방향과 강도를 하나의 값으로 요약.

```text
-1 ≤ r ≤ 1
```

- `r > 0` → 함께 증가
- `r < 0` → 한쪽이 증가하면 다른 쪽 감소
- `|r|`이 1에 가까움 → 강한 선형관계
- `r`이 0에 가까움 → 약한 선형관계

상관은 관계를 나타낼 뿐 **인과관계를 증명하지 않음**.

## 8.2 회귀

한 변수 `x`를 이용해 다른 변수 `y`를 설명하거나 예측.

```text
y = ax + b
```

| 구분 | 상관 | 회귀 |
|---|---|---|
| 질문 | 얼마나 함께 움직이는가 | x가 변할 때 y가 얼마나 변하는가 |
| 결과 | 상관계수 `r` | 회귀식과 계수 |
| 변수 역할 | 두 변수가 대등 | 독립변수 x와 종속변수 y 구분 |
| x·y 교환 | r은 같음 | 회귀식은 달라짐 |

---

# 9. 단순 선형회귀

```python
import statsmodels.formula.api as smf

model = smf.ols(
    'mpg ~ weight',
    data=df
).fit()

print(model.summary())
```

## 9.1 최소제곱법 OLS

```text
잔차 = 실제값 - 예측값
```

OLS는 잔차 제곱합을 가장 작게 만드는 회귀선을 선택.

## 9.2 결과표 핵심

| 항목 | 의미 |
|---|---|
| `coef` | x가 1 증가할 때 y의 평균 변화량 |
| `P>|t|` | 해당 계수가 0인지 검정한 p-value |
| `R-squared` | y 변동 중 모형이 설명한 비율 |
| `Intercept` | x가 0일 때 예측되는 y |

예:

```text
weight coef = -0.0076
→ 무게가 1파운드 늘 때 연비가 평균 0.0076mpg 감소
→ 100파운드 늘 때 약 0.76mpg 감소
```

## 9.3 R² 해석

```text
R² = 모형이 설명한 종속변수 변동의 비율
```

예:

```text
R² = 0.693
→ 연비 변동의 약 69.3%를 무게가 설명
```

주의:

- R²가 높아도 인과관계가 증명되는 것은 아님
- 높은 R²가 항상 좋은 예측을 보장하지 않음
- 관측 범위 밖 예측인 **외삽**은 위험
- `R² = r²`는 **절편이 포함된 단순 선형회귀**에서만 성립

---

# 10. 다중 선형회귀

여러 독립변수로 하나의 종속변수를 함께 설명.

```python
model = smf.ols(
    'mpg ~ weight + horsepower + displacement',
    data=df
).fit()
```

식:

```text
mpg = a
    + b₁ × weight
    + b₂ × horsepower
    + b₃ × displacement
```

## 10.1 계수 해석

다중회귀의 계수는 다음 의미.

```text
다른 독립변수를 고정했을 때
해당 변수가 1 증가하면 y가 평균적으로 얼마나 변하는가
```

예:

```text
weight 계수
→ 마력과 배기량이 같은 자동차끼리 비교했을 때
  무게가 1파운드 증가하면 연비가 얼마나 변하는가
```

## 10.2 범주형 변수

### 명목형 범주

순서가 없는 범주는 더미 변수로 변환.

```python
smf.ols(
    'mpg ~ weight + C(origin)',
    data=df
)
```

범주가 `k`개면 기준 범주 하나를 제외한 `k-1`개의 더미를 사용.

계수는 **기준 범주와의 차이**로 해석.

### 순서형 범주

```python
mapping = {
    '저': 0,
    '중': 1,
    '고': 2
}
```

정수로 넣으면 다음을 가정.

```text
저 → 중의 간격과
중 → 고의 간격이 같다
```

간격이 같다고 보기 어렵다면 원-핫 인코딩이 더 안전.

## 10.3 조정 R²

일반 R²는 의미 없는 변수를 추가해도 거의 항상 증가.

```text
조정 R²
= 변수 개수에 벌점을 적용한 설명력
```

변수 개수가 다른 모형을 비교할 때는 조정 R²를 함께 확인.

## 10.4 다중공선성

독립변수끼리 강하게 관련되면 각 계수의 해석이 불안정해짐.

```text
VIFᵢ = 1 ÷ (1 - Rᵢ²)
```

`Rᵢ²`는 특정 독립변수 `i`를 나머지 독립변수들로 설명했을 때의 결정계수.

| VIF | 해석 |
|---:|---|
| 1에 가까움 | 안정적 |
| 5 이상 | 주의 |
| 10 이상 | 강한 다중공선성 의심 |

VIF 기준은 절대 규칙이 아니라 실무 판단 기준.

---

# 11. 회귀 진단

회귀 가정은 원자료보다 **잔차**를 이용해 점검.

| 가정 | 뜻 | 점검 방법 |
|---|---|---|
| 선형성 | 관계가 직선에 가까움 | 잔차 vs 적합값에 곡선 무늬가 없는지 |
| 등분산성 | 잔차 퍼짐이 일정함 | 깔때기 모양 없이 고르게 퍼지는지 |
| 정규성 | 잔차가 정규분포에 가까움 | 잔차 Q-Q Plot |
| 독립성 | 잔차끼리 상관이 없음 | Durbin-Watson이 2 근처인지 |

## 11.1 잔차 vs 적합값

한 그림으로 두 가지 확인.

```text
곡선 무늬 → 선형성 위배
깔때기 무늬 → 등분산성 위배
```

## 11.2 잔차 Q-Q Plot

```text
점이 직선을 따름 → 잔차 정규성에 가까움
꼬리가 크게 휨 → 이상치·비정규 가능성
```

## 11.3 Durbin-Watson

범위는 대략 `0~4`.

```text
2 근처 → 1차 자기상관이 뚜렷하지 않음
0에 가까움 → 양의 자기상관 가능성
4에 가까움 → 음의 자기상관 가능성
```

특히 시간·관측 순서가 있는 자료에서 중요.

## 11.4 가정이 깨졌을 때

| 위배 | 주된 문제 | 대응 |
|---|---|---|
| 선형성 | 계수와 예측 자체가 잘못될 수 있음 | 로그 변환·제곱항·비선형 모형 |
| 등분산성 | 표준오차·p-value·신뢰구간이 흔들림 | 로그 변환·강건 표준오차 |
| 정규성 | 작은 표본에서 추론이 불안정 | 이상치 확인·변환·표본 크기 고려 |
| 독립성 | p-value가 과도하게 작아질 수 있음 | 빠진 순서 변수 추가·시계열 방법·강건 표준오차 |

> 네 가정은 단순한 통과·실패가 아님.  
> 분석 목적이 예측인지, 계수 해석인지, 유의성 검정인지에 따라 중요도가 달라짐.

---

# 12. 효과크기 총정리

| 상황 | 검정 | 효과크기 |
|---|---|---|
| 두 평균 비교 | t-검정 | Cohen's d·Hedges' g |
| 독립 두 집단 순위 비교 | Mann-Whitney U | RBC |
| 대응·1표본 순위 비교 | Wilcoxon | RBC |
| 세 집단 이상 평균 비교 | ANOVA | η²·부분 η² |
| 세 집단 이상 순위 비교 | Kruskal-Wallis | 사후 쌍별 RBC 등 |
| 범주 1개 빈도 | 카이제곱 적합도 | Cohen's w |
| 범주 2개 관계 | 카이제곱 독립성 | Cramér's V |
| 회귀 | 선형회귀 | R²·조정 R² |

> 효과크기 기준은 분야별로 다를 수 있으므로 절대 규칙으로 사용하지 않음.

---

# 13. 결과 해석 순서

통계 결과는 다음 순서로 읽으면 됨.

```text
1. 분석 질문과 가설 확인
2. 사용한 검정과 가정 확인
3. p-value로 통계적 유의성 확인
4. 효과크기로 실제 차이의 크기 확인
5. 신뢰구간이 있다면 불확실성 확인
6. 전체 검정이 유의하면 사후검정
7. 통계적 의미와 실제 의미를 나눠 결론 작성
```

예:

```text
Welch t-검정 결과 두 집단의 평균 차이는 통계적으로 유의했다
(p = 0.012).

그러나 효과크기는 작았다
(Hedges' g = 0.24).

따라서 차이는 존재하지만 실제 차이의 크기는 크지 않다고 해석한다.
```

---

# 14. 최종 핵심 요약

```text
p-value
→ 차이가 없다고 가정했을 때 현재 결과가 얼마나 드문가

효과크기
→ 실제 차이가 얼마나 큰가

독립 두 집단
→ Student t 또는 Welch t
→ 정규성 위배가 크면 Mann-Whitney U

독립 세 집단 이상
→ 일반 ANOVA 또는 Welch ANOVA
→ 정규성 위배가 크면 Kruskal-Wallis

ANOVA 유의
→ 일반 ANOVA는 Tukey
→ Welch ANOVA는 Games-Howell
→ Kruskal-Wallis는 Dunn

범주형
→ 카이제곱
→ 기대빈도가 부족한 작은 2×2 표는 Fisher 정확검정

상관
→ 두 변수가 함께 움직이는 방향과 강도

회귀
→ x가 변할 때 y가 얼마나 변하는지 식으로 설명·예측

R²
→ 모형이 설명한 변동 비율
→ 인과관계의 증거는 아님
```
